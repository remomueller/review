$("#user_<%= @reviewer.id %>_notified_pp").html('<span class="muted">Reminder sent <%= simple_time(@user_publication_review.reminder_sent_at) %></span> <%= link_to 'Resend', send_reminder_publication_path(@publication, reviewer: @user_publication_review.user_id), method: :post, remote: true, class: 'btn btn-mini' %>');
$("#user_<%= @reviewer.id %>_notified_sc").html('<span class="muted">Reminder sent <%= simple_time(@user_publication_review.reminder_sent_at) %></span> <%= link_to 'Resend', send_reminder_publication_path(@publication, reviewer: @user_publication_review.user_id), method: :post, remote: true, class: 'btn btn-mini' %>');
