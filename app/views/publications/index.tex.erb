<%#= index.tex.erb %>
<%# publications %>

\documentclass{article}
\usepackage{graphicx}
\usepackage{fancyhdr}
\usepackage[margin=0.5in]{geometry}
\geometry{top=1.0in, bottom=1.0in}
\usepackage{lastpage}
\usepackage{wrapfig}
\usepackage[section]{placeins}
\usepackage{booktabs}
\usepackage{sectsty}
\newcommand{\ra}[1]{\renewcommand{\arraystretch}{#1}}
\usepackage{indentfirst}
\usepackage{longtable}


\usepackage{hyperref}
\usepackage{pifont}

\usepackage{needspace}
\usepackage{color}

\fancyhf{}

\lhead{}
\chead{\textbf{<%= latex_safe "CHAT Publications" %>}}
\rhead{}
\lfoot{<%= latex_safe "Publication Matrix" %>}
\cfoot{<%= latex_safe Date.today.strftime("%B %-d, %Y") %>}
\rfoot{\thepage\ of \pageref{LastPage}}


  \renewcommand{\headrulewidth}{0pt}
  \renewcommand{\footrulewidth}{0pt}
  \setlength\headheight{22pt}

\pagestyle{fancyplain}



\begin{document}


\begin{longtable}{lllrcrrr}\toprule
<% publications.each do |publication| %>
  <%= latex_safe publication.manuscript_number %> &
  <%= latex_safe publication.abbreviated_title.to_s[0..10] %> &
  <%= latex_safe publication.user.name if publication.user %> &
  <%= latex_safe publication.publication_type %> &
  <%= latex_safe publication.status %> &
  <%= latex_safe publication.dataset_requested_analyst %> &
  <%= latex_safe publication.targeted_start_date.strftime("%b %d, %Y (%a)") if publication.targeted_start_date %> \\
  & & \multicolumn{5}{l}{ <%= latex_safe publication.finalized_nominations.join(', ') %> } \\
  & \multicolumn{6}{l}{ <%= latex_safe publication.secretary_notes %> } \\
  \\
  \midrule
<% end %>
\end{longtable}


\end{document}

