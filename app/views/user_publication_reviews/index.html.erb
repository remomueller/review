<h1>Listing user_publication_reviews</h1>

<table>
  <tr>
    <th>User</th>
    <th>Publication</th>
    <th>Status</th>
    <th>Comment</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @user_publication_reviews.each do |user_publication_review| %>
  <tr>
    <td><%= user_publication_review.user_id %></td>
    <td><%= user_publication_review.publication_id %></td>
    <td><%= user_publication_review.status %></td>
    <td><%= user_publication_review.comment %></td>
    <td><%= link_to 'Show', user_publication_review %></td>
    <td><%= link_to 'Edit', edit_user_publication_review_path(user_publication_review) %></td>
    <td><%= link_to 'Destroy', user_publication_review, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User publication review', new_user_publication_review_path %>
